<div>
    <div>
  <% if post.likes.exists?(user_id: @current_user) %>
    <p> Post liked! </p>
  <% else %>
    <%= form_with url: like_form_path(post.id) do |form| %>
      <%= form.submit "Like" %>
    <% end %>
  <% end %>
</div>
</div>